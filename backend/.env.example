# Copy to .env and fill in values
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
GROQ_API_KEY=
LLAMA_CLOUD_API_KEY=    # optional — falls back to pypdf when unset
TAVILY_API_KEY=         # optional — falls back to DuckDuckGo when unset
